import math
import os
import random
import re
import sys

def weird(num):
    if num>20 and num%2==0 or 2<num<5 and num%2==0:
        return "Not Weird"
    else:
        return "Weird"

if __name__ == '__main__':
    n = int(input().strip())
    print(weird(n))
